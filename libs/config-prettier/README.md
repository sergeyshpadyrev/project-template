# `@repo/config-prettier`

Shared internal Prettier configuration.

## Usage

Add the package as a dev dependency and reference it from a `prettier.config.mjs`:

```js
export { default } from "@repo/config-prettier";
```
